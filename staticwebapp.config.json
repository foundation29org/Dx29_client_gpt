{
    "routes": [
      {
        "route": "/api/*",
        "rewrite": "https://dxgpt.azurewebsites.net/api/$1",
        "methods": [ "GET", "POST", "PUT", "DELETE", "OPTIONS" ],
        "allowedRoles": [ "anonymous" ]
      }
    ]
  }